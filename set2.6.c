#include<stdio.h>
#include<conio.h>
void main()
{
int n,a[100],count=0,i,j;
clrscr();
scanf("%d",&n);
for(i=0;i<n;i++)
{
    scanf("%d",&a[i]);
}
for(i=0;i<n;i++)
{
    count=0;
    for(j=i+1;j<n;j++)
    {
        if(a[i]==a[j])
        {
            count++;
            a[j]='&';
        }
    }
    if(count==0)
    {
        printf("%d",a[i]);
        break;
    }
}
getch();
}
